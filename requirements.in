# Direct dependencies — compile with pip-compile to produce requirements.txt
# pip-compile requirements.in --output-file requirements.txt

discord.py[voice]>=2.3.0
httpx>=0.27.0
pydantic-settings>=2.3.0
aiohttp>=3.10.0
redis>=5.0.0
